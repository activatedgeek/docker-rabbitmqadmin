#!/usr/bin/env sh
if [ -n  "${RABBIT_ECHO+1}" ]; then
  echo "'$@' against $RABBIT_HOST:$RABBIT_PORT:"
fi

rabbitmqctl --node "rabbit@$RABBIT_HOST" --longnames --erlang-cookie $RABBIT_ERLANG_COOKIE  $@
